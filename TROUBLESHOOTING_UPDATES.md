# 🔧 Отладка системы обновления лаунчера

## Проблема: Лаунчер не обновляется автоматически

### Шаг 1: Проверка версии в БД

Убедитесь, что версия добавлена в базу данных:

```sql
SELECT * FROM launcher_versions WHERE enabled = 1 ORDER BY createdAt DESC;
```

Проверьте:
- ✅ Версия существует
- ✅ `enabled = 1`
- ✅ `downloadUrl` заполнен и доступен
- ✅ Версия новее текущей версии лаунчера

### Шаг 2: Проверка текущей версии лаунчера

В консоли разработчика (F12) проверьте:
```javascript
await window.electronAPI.getAppVersion()
```

Или в логах Electron main process:
```
[LauncherUpdate] Current version: 1.0.133
```

### Шаг 3: Проверка API endpoint

Проверьте, что API работает:

```bash
curl "http://localhost:7240/api/launcher/check-update?currentVersion=1.0.133"
```

Должен вернуть:
```json
{
  "success": true,
  "data": {
    "hasUpdate": true,
    "currentVersion": "1.0.133",
    "latestVersion": {
      "version": "1.0.134",
      "downloadUrl": "https://...",
      ...
    }
  }
}
```

### Шаг 4: Проверка логов

Откройте DevTools (F12) и проверьте консоль. Должны быть логи:
```
[LauncherUpdate] Current version: 1.0.133
[LauncherUpdate] Checking for updates... (current: 1.0.133, API: http://localhost:7240)
[LauncherUpdate] Update check result: {...}
```

### Шаг 5: Проверка переменной окружения

Убедитесь, что `VITE_API_URL` правильно настроен:

1. Создайте файл `packages/frontend/.env`:
```env
VITE_API_URL=http://localhost:7240
```

2. Пересоберите лаунчер:
```bash
npm run build
```

### Шаг 6: Ручная проверка обновлений

Добавьте кнопку проверки обновлений на страницу настроек (см. `UpdateCheckButton.tsx`).

Или в консоли разработчика:
```javascript
// Получить хук (если доступен)
// Или напрямую через API
const result = await window.electronAPI.checkLauncherUpdate('1.0.133', 'http://localhost:7240');
console.log(result);
```

## Частые проблемы

### 1. API URL неправильный

**Проблема**: Используется `http://localhost:7240` вместо реального сервера.

**Решение**: 
- Создайте `.env` файл с правильным URL
- Или используйте переменную окружения при сборке

### 2. Версия в БД не найдена

**Проблема**: Версия в БД не совпадает с версией в package.json.

**Решение**: 
```sql
-- Проверьте версию
SELECT version FROM launcher_versions WHERE enabled = 1;

-- Убедитесь, что версия правильная
UPDATE launcher_versions SET version = '1.0.134' WHERE version = '1.0.133';
```

### 3. Download URL недоступен

**Проблема**: Файл не доступен по указанному URL.

**Решение**:
```bash
# Проверьте доступность
curl -I https://your-server.com/launcher-1.0.134-Setup.exe

# Должен вернуть HTTP 200
```

### 4. Проверка не запускается

**Проблема**: Автоматическая проверка не срабатывает.

**Решение**: 
- Проверьте логи в консоли
- Убедитесь, что `currentVersion` получен
- Проверка запускается через 3 секунды после получения версии

### 5. Модальное окно не показывается

**Проблема**: Обновление найдено, но модальное окно не появляется.

**Решение**:
- Проверьте логи: `[App] Update available, showing modal`
- Убедитесь, что `updateCheckResult.hasUpdate === true`
- Проверьте, что `updateCheckResult.updateInfo` существует

## Отладка через DevTools

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Найдите логи с префиксом `[LauncherUpdate]`
4. Проверьте:
   - Текущая версия
   - URL запроса
   - Ответ сервера
   - Результат проверки

## Тестирование обновления

1. **Создайте тестовую версию**:
```sql
INSERT INTO launcher_versions (id, version, downloadUrl, enabled, isRequired, createdAt)
VALUES (UUID(), '999.999.999', 'https://example.com/test.exe', 1, 0, NOW());
```

2. **Измените версию в package.json** на старую (например, `1.0.0`)

3. **Пересоберите лаунчер**

4. **Запустите** - должно появиться уведомление об обновлении

## Проверка через SQL

```sql
-- Все версии
SELECT version, downloadUrl, enabled, isRequired, createdAt 
FROM launcher_versions 
ORDER BY createdAt DESC;

-- Последняя включенная версия
SELECT version, downloadUrl 
FROM launcher_versions 
WHERE enabled = 1 
ORDER BY createdAt DESC 
LIMIT 1;

-- Проверка версии для обновления с 1.0.133
SELECT * FROM launcher_versions 
WHERE enabled = 1 
AND version > '1.0.133'
ORDER BY version DESC 
LIMIT 1;
```

## Контакты для поддержки

Если проблема не решена:
1. Соберите логи из консоли DevTools
2. Проверьте логи Electron main process
3. Проверьте ответ API endpoint
4. Убедитесь, что все версии правильные

