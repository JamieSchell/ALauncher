# Contributing to Modern Minecraft Launcher

Спасибо за интерес к проекту! Мы приветствуем вклад от всех участников.

## 📋 Содержание

- [Code of Conduct](#code-of-conduct)
- [Как внести вклад](#как-внести-вклад)
- [Code Style](#code-style)
- [Git Workflow](#git-workflow)
- [Pull Request Process](#pull-request-process)
- [Требования к тестам](#требования-к-тестам)
- [Требования к документации](#требования-к-документации)

## Code of Conduct

Этот проект следует принципам открытого и уважительного общения. Мы ожидаем, что все участники будут:

- Быть уважительными и профессиональными
- Принимать конструктивную критику
- Фокусироваться на том, что лучше для сообщества
- Проявлять эмпатию к другим участникам

## Как внести вклад

### Сообщения о багах

Перед созданием issue о баге:

1. Проверьте, не был ли баг уже зарегистрирован
2. Убедитесь, что используете последнюю версию
3. Попробуйте воспроизвести проблему

При создании issue укажите:

- **Версию лаунчера**: Из `package.json` или About
- **Версию Node.js**: `node --version`
- **Операционную систему**: Windows/Linux/macOS и версия
- **Описание проблемы**: Что происходит
- **Шаги для воспроизведения**: Как воспроизвести баг
- **Ожидаемое поведение**: Что должно происходить
- **Фактическое поведение**: Что происходит на самом деле
- **Скриншоты/логи**: Если применимо

### Предложения новых функций

1. Создайте issue с описанием функции
2. Обсудите с мейнтейнерами перед началом работы
3. Дождитесь одобрения перед созданием PR

### Исправление багов

1. Найдите issue с багом
2. Закомментируйте, что вы работаете над ним
3. Создайте ветку от `develop` (или `main` если нет `develop`)
4. Исправьте баг и добавьте тесты
5. Создайте Pull Request

## Code Style

### TypeScript

- Используйте TypeScript для всех новых файлов
- Избегайте `any` - используйте конкретные типы или `unknown`
- Используйте интерфейсы для объектов, типы для unions/intersections
- Предпочитайте `const` и `let` вместо `var`
- Используйте стрелочные функции для коротких функций

```typescript
// ✅ Хорошо
interface User {
  id: string;
  username: string;
}

const getUser = async (id: string): Promise<User | null> => {
  // ...
};

// ❌ Плохо
const getUser = async (id: any): Promise<any> => {
  // ...
};
```

### React

- Используйте функциональные компоненты и хуки
- Называйте компоненты с заглавной буквы
- Используйте `React.memo` для оптимизации при необходимости
- Избегайте вложенных компонентов в render

```typescript
// ✅ Хорошо
export const UserCard: React.FC<UserCardProps> = ({ user }) => {
  return <div>{user.username}</div>;
};

// ❌ Плохо
export function userCard({ user }) {
  return <div>{user.username}</div>;
}
```

### Именование

- **Файлы**: `camelCase` для утилит, `PascalCase` для компонентов
  - `userService.ts`
  - `UserCard.tsx`
- **Переменные/функции**: `camelCase`
  - `getUserById`
  - `isAuthenticated`
- **Константы**: `UPPER_SNAKE_CASE`
  - `MAX_RETRY_ATTEMPTS`
  - `API_BASE_URL`
- **Типы/Интерфейсы**: `PascalCase`
  - `UserProfile`
  - `ApiResponse`

### Форматирование

Проект использует **Prettier** для автоматического форматирования:

```bash
# Проверить форматирование
npm run format:check

# Автоматически исправить
npm run format
```

**Настройки Prettier:**
- Single quotes
- Semicolons
- 100 символов на строку
- 2 пробела для отступов

### Линтинг

Проект использует **ESLint** для проверки кода:

```bash
# Проверить код
npm run lint

# Автоматически исправить
npm run lint:fix
```

**Основные правила:**
- `@typescript-eslint/no-explicit-any`: предупреждение
- `@typescript-eslint/no-unused-vars`: предупреждение
- `no-var`: ошибка
- `prefer-const`: предупреждение

## Git Workflow

### Ветки

- `main` - стабильная версия, готовая к production
- `develop` - ветка разработки (если используется)
- `feature/*` - новые функции
- `fix/*` - исправления багов
- `docs/*` - изменения документации
- `refactor/*` - рефакторинг кода

### Создание ветки

```bash
# От develop (или main)
git checkout develop
git pull origin develop
git checkout -b feature/my-new-feature
```

### Коммиты

Используйте понятные сообщения коммитов:

```bash
# ✅ Хорошо
git commit -m "feat: add user profile page"
git commit -m "fix: resolve authentication token refresh issue"
git commit -m "docs: update API documentation"

# ❌ Плохо
git commit -m "fix"
git commit -m "update"
git commit -m "WIP"
```

**Формат:** `<type>: <subject>`

**Типы:**
- `feat`: новая функция
- `fix`: исправление бага
- `docs`: изменения документации
- `style`: форматирование (не влияет на код)
- `refactor`: рефакторинг
- `test`: добавление/изменение тестов
- `chore`: изменения в build процессе, зависимостях

### Pre-commit Hooks

Проект использует **Husky** для автоматических проверок перед коммитом:

- ESLint проверка
- Prettier форматирование
- Проверка типов (опционально)

Коммит будет заблокирован, если проверки не пройдут.

## Pull Request Process

### Перед созданием PR

1. ✅ Убедитесь, что код проходит линтинг: `npm run lint`
2. ✅ Проверьте форматирование: `npm run format:check`
3. ✅ Проверьте типы: `npm run typecheck`
4. ✅ Убедитесь, что все тесты проходят: `npm test`
5. ✅ Обновите документацию, если нужно
6. ✅ Обновите CHANGELOG, если нужно

### Создание PR

1. **Заголовок PR:**
   - Используйте тот же формат, что и коммиты
   - Пример: `feat: add user profile page`

2. **Описание PR:**
   - Опишите, что изменено и почему
   - Укажите связанные issues: `Closes #123`
   - Добавьте скриншоты, если применимо
   - Опишите breaking changes, если есть

3. **Чеклист:**
   - [ ] Код следует style guide проекта
   - [ ] Я провел self-review своего кода
   - [ ] Я прокомментировал сложные части кода
   - [ ] Я обновил документацию
   - [ ] Мои изменения не генерируют новых предупреждений
   - [ ] Я добавил тесты, которые доказывают, что мой fix эффективен или что моя функция работает
   - [ ] Новые и существующие unit тесты проходят локально

### Review Process

1. Мейнтейнеры проверят PR
2. Могут быть запрошены изменения
3. После одобрения PR будет смержен
4. Не делайте force push после начала review (используйте дополнительные коммиты)

## Требования к тестам

### Когда писать тесты

- ✅ Для новых функций
- ✅ Для исправлений багов (регрессионные тесты)
- ✅ Для критичной бизнес-логики
- ❌ Для простых UI компонентов без логики
- ❌ Для утилит, которые тривиальны

### Типы тестов

1. **Unit тесты**: Тестирование отдельных функций/компонентов
2. **Integration тесты**: Тестирование взаимодействия компонентов
3. **E2E тесты**: Тестирование полных пользовательских сценариев (если настроены)

### Примеры

```typescript
// Unit test example
describe('calculateFileHash', () => {
  it('should calculate SHA-256 hash correctly', async () => {
    const hash = await calculateFileHash('test-file.txt');
    expect(hash).toBe('expected-hash');
  });
});

// Integration test example
describe('UserService', () => {
  it('should create user and return JWT token', async () => {
    const user = await userService.create({ username: 'test', password: 'pass' });
    expect(user).toHaveProperty('accessToken');
  });
});
```

## Требования к документации

### Когда обновлять документацию

- ✅ При добавлении новых функций
- ✅ При изменении API
- ✅ При изменении процесса сборки/развертывания
- ✅ При добавлении новых зависимостей
- ✅ При изменении архитектуры

### Что документировать

1. **Код:**
   - JSDoc комментарии для публичных функций
   - Комментарии для сложной логики
   - README для новых модулей

2. **API:**
   - Обновлять API документацию в README.md
   - Описывать новые endpoints
   - Указывать breaking changes

3. **Архитектура:**
   - ADR документы для важных решений
   - Обновлять архитектурные диаграммы

### Формат JSDoc

```typescript
/**
 * Calculates SHA-256 hash of a file
 * 
 * @param filePath - Path to the file
 * @param algorithm - Hash algorithm (default: 'sha256')
 * @returns Promise resolving to hex hash string
 * @throws {Error} If file cannot be read
 * 
 * @example
 * ```typescript
 * const hash = await calculateFileHash('file.txt');
 * console.log(hash); // 'abc123...'
 * ```
 */
export async function calculateFileHash(
  filePath: string,
  algorithm: string = 'sha256'
): Promise<string> {
  // ...
}
```

## Вопросы?

Если у вас есть вопросы:

1. Проверьте существующую документацию
2. Поищите в issues
3. Создайте новый issue с вопросом
4. Свяжитесь с мейнтейнерами

## Благодарности

Спасибо всем, кто вносит вклад в проект! 🎉

---

**Последнее обновление:** 2025-01-30

