# 🎮 Modern Minecraft Launcher

Современный, полнофункциональный лаунчер для Minecraft, разработанный с использованием React, Electron и Node.js. Профессиональное решение для управления игровыми серверами, версиями клиента, игроками и статистикой.

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-GPL%20v3.0-green.svg)
![Node](https://img.shields.io/badge/node-%3E%3D20.0.0-brightgreen.svg)
![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-blue.svg)

---

## 📋 Содержание

- [Основные возможности](#-основные-возможности)
- [Архитектура проекта](#-архитектура-проекта)
- [Технологический стек](#-технологический-стек)
- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [API документация](#-api-документация)
- [CLI система управления](#-cli-система-управления)
- [Разработка](#-разработка)
- [Сборка и развертывание](#-сборка-и-развертывание)
- [Безопасность](#-безопасность)
- [Устранение неполадок](#-устранение-неполадок)
- [Вклад в проект](#-вклад-в-проект)

---

## ✨ Основные возможности

### 🎯 Управление серверами

- **📊 Мониторинг в реальном времени** - отслеживание статуса, онлайна и пинга серверов
- **📈 Статистика за 24 часа** - детальные графики онлайна игроков с интервалом 5 минут
- **🏷️ Система тегов** - цветные бейджи для серверов (NEW, TOP, WIP, HARD, POPULAR и др.)
- **🔍 Детальная информация** - страница с полной информацией о каждом сервере
- **🌐 Поддержка всех версий** - от Minecraft 1.2.5 до последней версии
- **💹 Экономика** - интеграция с API экономики для отображения топа игроков

### 📦 Управление версиями клиента

- **🔄 Автоматическое скачивание** - загрузка клиентских файлов через WebSocket с прогресс-баром
- **✅ Проверка целостности** - валидация файлов через SHA-256 хеши
- **📁 Управление библиотеками** - автоматическая обработка LWJGL и других нативных библиотек
- **🎮 Множественные версии** - поддержка нескольких версий Minecraft одновременно
- **🔧 Поддержка модлоадеров** - Vanilla, Forge (1.12.2+), Fabric (1.16+)
- **📥 Автоматическая установка** - автоматическое скачивание и установка Forge/Fabric loader
- **🌍 Поддержка Java версий** - автоматический выбор Java 8/17/21 в зависимости от версии Minecraft

### 🚀 Запуск игры

- **🎯 Единая кнопка запуска** - автоматическая проверка и загрузка файлов перед запуском
- **📊 Прогресс загрузки** - красивое модальное окно с детальной информацией о процессе
- **📝 Логи игры** - отображение логов Minecraft в реальном времени
- **🔧 Автоматическая настройка** - правильная конфигурация JVM и нативных библиотек
- **⚡ Оптимизация производительности** - настройка JVM аргументов для каждой версии

### 🔐 Безопасность и авторизация

- **🔒 JWT авторизация** - безопасная система входа с токенами доступа и обновления
- **🔑 RSA шифрование** - защита критичных данных при передаче
- **🛡️ Rate limiting** - защита от брутфорса и злоупотреблений
- **🔐 bcrypt хеширование** - безопасное хранение паролей (10 rounds)
- **🚫 Система банов** - возможность банить пользователей с указанием причины
- **👥 Роли пользователей** - система ролей (USER, ADMIN)
- **📋 Whitelist/Blacklist** - управление доступом по IP адресам

### 💻 Интерфейс

- **🎨 Современный UI** - красивый интерфейс на React + TailwindCSS
- **🎭 Анимации** - плавные переходы с Framer Motion
- **📱 Адаптивный дизайн** - работает на разных размерах экрана
- **🌙 Темная тема** - комфортная работа в любое время суток
- **🌍 Мультиязычность** - поддержка нескольких языков (i18n)
- **📊 Визуализация данных** - графики и диаграммы с Recharts

### 📢 Уведомления

- **🔔 Система уведомлений** - отправка уведомлений пользователям
- **📬 Типы уведомлений** - CLIENT_UPDATE_AVAILABLE, SERVER_STATUS_CHANGE, LAUNCHER_UPDATE_AVAILABLE и др.
- **👤 Персональные уведомления** - отправка конкретному пользователю или всем
- **📋 История уведомлений** - отслеживание прочитанных/непрочитанных уведомлений

### 📊 Статистика и аналитика

- **📈 Статистика запусков** - отслеживание количества запусков игры
- **💥 Статистика крашей** - мониторинг ошибок и крашей игры
- **⏱️ Игровые сессии** - отслеживание времени игры
- **❌ Ошибки лаунчера** - логирование и анализ ошибок
- **👥 Статистика пользователей** - количество активных, забаненных, администраторов

### 🛠️ CLI система управления

- **💻 Командная строка** - полнофункциональная CLI для управления лаунчером
- **👥 Управление пользователями** - создание, удаление, баны, изменение ролей
- **📦 Управление профилями** - создание и настройка профилей клиентов
- **📥 Автоматическое скачивание клиентов** - команда для скачивания Vanilla/Forge/Fabric клиентов
- **📊 Статистика** - просмотр статистики через CLI
- **🔔 Управление уведомлениями** - отправка уведомлений через CLI

---

## 🏗️ Архитектура проекта

Проект построен на основе **monorepo** архитектуры с использованием npm workspaces. Код разделен на три основных пакета, каждый из которых имеет четко определенную ответственность и может разрабатываться независимо.

### Архитектурные принципы

- **Разделение ответственности**: Каждый пакет отвечает за свою область
- **Переиспользование**: Общие типы и утилиты в `shared`
- **Типобезопасность**: Полная типизация через TypeScript и `@modern-launcher/shared`
- **Модульность**: Легко добавлять новые функции без изменения существующего кода

### Структура проекта

```
.
├── packages/
│   ├── frontend/              # Electron + React приложение
│   │   ├── electron/         # Main process и preload скрипты
│   │   │   ├── main.ts       # Главный процесс Electron (управление окнами, IPC)
│   │   │   └── preload.ts    # Preload скрипты для безопасного IPC моста
│   │   ├── src/
│   │   │   ├── api/          # API клиенты (axios) для взаимодействия с backend
│   │   │   ├── components/   # React компоненты
│   │   │   │   ├── common/   # Общие переиспользуемые компоненты
│   │   │   │   ├── server/   # Компоненты для работы с серверами
│   │   │   │   ├── layout/   # Компоненты макета (Header, Sidebar, etc.)
│   │   │   │   └── ...
│   │   │   ├── pages/         # Страницы приложения (роуты)
│   │   │   │   ├── LoginPage.tsx
│   │   │   │   ├── HomePage.tsx
│   │   │   │   ├── ServerDetailsPage.tsx
│   │   │   │   └── ...
│   │   │   ├── hooks/         # Кастомные React хуки
│   │   │   ├── stores/       # Zustand хранилища (глобальное состояние)
│   │   │   ├── services/     # Сервисы (WebSocket, файлы, уведомления)
│   │   │   ├── utils/        # Утилиты (форматирование, валидация)
│   │   │   ├── config/       # Конфигурация (API URLs, Electron settings)
│   │   │   ├── i18n/         # Интернационализация (переводы)
│   │   │   └── types/        # Локальные TypeScript типы
│   │   ├── vite.config.ts    # Конфигурация Vite
│   │   └── package.json
│   │
│   ├── backend/              # Node.js + Express сервер
│   │   ├── src/
│   │   │   ├── index.ts      # Точка входа сервера
│   │   │   ├── config/       # Конфигурация (env переменные)
│   │   │   ├── routes/       # API endpoints (REST API)
│   │   │   │   ├── auth.ts   # Авторизация (login, register, refresh)
│   │   │   │   ├── profiles.ts # Управление профилями серверов
│   │   │   │   ├── servers.ts # Статус и статистика серверов
│   │   │   │   ├── clientVersions.ts # Версии клиентов Minecraft
│   │   │   │   ├── updates.ts # Обновления клиентов
│   │   │   │   ├── users.ts  # Управление пользователями
│   │   │   │   ├── notifications.ts # Система уведомлений
│   │   │   │   ├── statistics.ts # Статистика запусков, крашей
│   │   │   │   ├── crashes.ts # Обработка крашей игры
│   │   │   │   └── launcher.ts # Обновления лаунчера
│   │   │   ├── services/     # Бизнес-логика (application layer)
│   │   │   │   ├── auth.ts   # Логика авторизации
│   │   │   │   ├── database.ts # Prisma клиент и helpers
│   │   │   │   ├── crypto.ts # RSA шифрование, хеширование
│   │   │   │   ├── fileSyncService.ts # Синхронизация файлов
│   │   │   │   ├── clientDownloadService.ts # Скачивание клиентов
│   │   │   │   ├── serverPing.ts # Пинг Minecraft серверов
│   │   │   │   ├── serverStatistics.ts # Сбор статистики серверов
│   │   │   │   ├── notificationService.ts # Отправка уведомлений
│   │   │   │   └── ...
│   │   │   ├── middleware/   # Express middleware
│   │   │   │   ├── auth.ts   # JWT авторизация
│   │   │   │   ├── errorHandler.ts # Обработка ошибок
│   │   │   │   └── rateLimiter.ts # Rate limiting
│   │   │   ├── websocket/    # WebSocket сервер (real-time обновления)
│   │   │   │   └── index.ts
│   │   │   ├── utils/        # Утилиты
│   │   │   │   ├── logger.ts # Логирование
│   │   │   │   └── ...
│   │   │   └── cli/          # CLI система управления
│   │   │       └── index.ts
│   │   ├── prisma/           # Prisma схема и миграции
│   │   │   └── schema.prisma # Схема базы данных
│   │   ├── updates/         # Файлы клиентов Minecraft
│   │   │   ├── <profile-name>/ # Директория для каждого профиля
│   │   │   │   ├── client.jar # Основной jar файл
│   │   │   │   ├── libraries/ # Нативные библиотеки
│   │   │   │   ├── mods/     # Моды (если есть)
│   │   │   │   └── ...
│   │   │   └── assets/      # Общие ресурсы (текстуры, звуки)
│   │   └── package.json
│   │
│   └── shared/               # Общие типы и утилиты
│       └── src/
│           ├── types/        # TypeScript типы (API, Electron, общие)
│           │   ├── index.ts  # Экспорт всех типов
│           │   ├── api.ts    # Типы для API запросов/ответов
│           │   ├── electron.ts # Типы для Electron IPC
│           │   └── ...
│           └── utils/        # Общие утилиты (если есть)
│
├── scripts/                  # Скрипты сборки и развертывания
├── build-scripts/            # Документация по сборке
│   ├── CLI_README.md
│   └── CLIENT_BUILD_GUIDE.md
└── package.json              # Корневой package.json
```

### Компоненты системы

#### Frontend (Electron + React)

- **Main Process** (`electron/main.ts`) - главный процесс Electron, управляет окнами и нативными API
- **Renderer Process** (`src/`) - React приложение, пользовательский интерфейс
- **Preload Scripts** (`electron/preload.ts`) - безопасный мост между main и renderer процессами
- **API Client** (`src/api/`) - HTTP клиент для взаимодействия с backend
- **WebSocket Client** (`src/services/`) - WebSocket клиент для real-time обновлений
- **State Management** (`src/stores/`) - Zustand хранилища для управления состоянием
- **Routing** - React Router для навигации между страницами

#### Backend (Node.js + Express)

- **REST API** (`src/routes/`) - RESTful API endpoints
- **WebSocket Server** (`src/websocket/`) - WebSocket сервер для real-time обновлений
- **Services** (`src/services/`) - бизнес-логика и сервисы
- **Database** (`prisma/`) - Prisma ORM для работы с MySQL
- **CLI** (`src/cli/`) - командная строка для управления
- **Middleware** (`src/middleware/`) - Express middleware (auth, error handling, rate limiting)

#### Shared

- **Types** (`src/types/`) - общие TypeScript типы для frontend и backend
- **Utilities** (`src/utils/`) - общие утилиты, используемые в нескольких пакетах

**Назначение:**
- Обеспечивает консистентность типов между frontend и backend
- Избегает дублирования определений типов
- Централизованное место для общих интерфейсов API

### Потоки данных

1. **Авторизация**: Frontend → REST API → JWT токен → Сохранение в localStorage
2. **Загрузка файлов**: Frontend → WebSocket → Backend → Файловая система → WebSocket → Frontend
3. **Мониторинг серверов**: Frontend → REST API → Backend → Minecraft Server → Backend → Frontend
4. **Уведомления**: Backend → WebSocket → Frontend
5. **Статистика**: Frontend → REST API → Backend → Database → Backend → Frontend

---

## 🛠️ Технологический стек

### Frontend

| Технология | Версия | Назначение |
|------------|--------|------------|
| **React** | 19.2.0 | UI библиотека |
| **TypeScript** | 5.9.3 | Типизация |
| **Electron** | 39.2.4 | Десктопное приложение |
| **Vite** | 7.2.4 | Сборка и HMR |
| **TailwindCSS** | 4.1.17 | Utility-first CSS |
| **Zustand** | 5.0.8 | Управление состоянием |
| **React Query** | 5.90.11 | Работа с API и кеширование |
| **Framer Motion** | 12.23.24 | Анимации |
| **Recharts** | 3.4.1 | Графики и визуализация |
| **React Router** | 7.9.6 | Маршрутизация |
| **Lucide React** | 0.555.0 | Иконки |
| **Axios** | 1.13.2 | HTTP клиент |

### Backend

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Node.js** | 20+ | Серверная платформа |
| **TypeScript** | 5.9.3 | Типизация |
| **Express** | 5.1.0 | Веб-сервер |
| **Prisma** | 6.0.0 | ORM для работы с БД |
| **MySQL** | 8.0+ | База данных |
| **JWT** | 9.0.2 | Токены авторизации |
| **bcrypt** | 6.0.0 | Хеширование паролей |
| **WebSocket (ws)** | 8.18.3 | Real-time обновления |
| **node-rsa** | 1.1.1 | RSA шифрование |
| **minecraft-server-util** | 5.4.4 | Пинг Minecraft серверов |
| **express-rate-limit** | 8.2.1 | Защита от злоупотреблений |
| **Helmet** | 8.1.0 | Защита заголовков |
| **Multer** | 2.0.2 | Загрузка файлов |
| **Chokidar** | 5.0.0 | Мониторинг файловой системы |
| **Zod** | 4.1.13 | Валидация схем |
| **dotenv** | 17.2.3 | Управление переменными окружения |

### Инструменты разработки

- **tsx** (4.20.6) - выполнение TypeScript файлов
- **concurrently** (9.2.1) - параллельный запуск процессов
- **electron-builder** - сборка Electron приложений
- **Prisma Studio** - GUI для работы с БД
- **wait-on** (9.0.3) - ожидание готовности сервисов

---

## 🚀 Быстрый старт

### Требования

- **Node.js** >= 20.0.0 (рекомендуется 20.19+ или 22.12+ LTS)
- **npm** >= 10.0.0
- **MySQL** >= 8.0
- **Git**

### Установка

```bash
# 1. Клонирование репозитория
git clone https://github.com/xuviga/LauncherSchool-sashok724-v3-Fork.git
cd LauncherSchool-sashok724-v3-Fork

# 2. Установка зависимостей
npm install

# 3. Сборка shared пакета
npm run build:shared
```

### Настройка базы данных

1. **Создайте базу данных MySQL:**

```sql
CREATE DATABASE launcher_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'launcher_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON launcher_db.* TO 'launcher_user'@'localhost';
FLUSH PRIVILEGES;
```

2. **Настройте переменные окружения** в `packages/backend/.env`:

```env
NODE_ENV=development
PORT=7240
HOST=0.0.0.0
DATABASE_URL=mysql://launcher_user:your_password@localhost:3306/launcher_db
JWT_SECRET=your_super_secret_jwt_key_min_32_chars
JWT_EXPIRY=24h
```

3. **Примените миграции:**

```bash
cd packages/backend
npm run generate
npx prisma db push
```

### Создание тестовых данных

```bash
cd packages/backend

# Создание администратора через CLI
npm run cli
# В CLI:
user create admin admin123 --role ADMIN --email admin@example.com

# Или через скрипты:
npm run list-users
npm run create-profile-custom
```

### Запуск

#### Development режим

```bash
# Запуск в режиме разработки (backend + frontend одновременно)
npm run dev

# Или отдельно:
npm run dev:backend   # Backend на http://localhost:7240
npm run dev:frontend  # Frontend на http://localhost:5173
```

**Что происходит:**
- Backend запускается с hot reload через `tsx watch`
- Frontend запускается с Vite HMR (Hot Module Replacement)
- Оба сервиса автоматически перезагружаются при изменении файлов

#### Production режим

```bash
# Сборка и запуск для production
npm run prod

# Или отдельно:
npm run prod:backend   # Собирает и запускает backend
npm run prod:frontend  # Собирает frontend для production
```

**Важно для production:**
- Убедитесь, что настроены правильные переменные окружения (`.env.prod`)
- Backend должен быть собран перед запуском: `npm run build`
- Frontend собирается в `dist/` директорию

---

## ⚙️ Конфигурация

### Backend конфигурация

Все настройки backend считываются из файла `packages/backend/.env` через модуль `config/index.ts`.  
Рекомендуется иметь отдельные файлы для разных окружений:

- `packages/backend/.env.development` – локальная разработка  
- `packages/backend/.env.production` – продакшн  
- `packages/backend/.env` – может быть симлинком или копией нужного файла

**Основные переменные:**

| Имя                         | Пример                                               | Обязательна | Описание |
|-----------------------------|------------------------------------------------------|------------|---------|
| `NODE_ENV`                  | `development` / `production`                        | да         | Режим работы сервера |
| `PORT`                      | `7240`                                              | да         | Порт HTTP‑сервера |
| `HOST`                      | `0.0.0.0`                                           | да         | Адрес привязки сервера |
| `CORS_ORIGIN`               | `http://localhost:5173,http://localhost:4173`       | рек.       | Разрешённые origin'ы для CORS (через запятую) |
| `DATABASE_URL`              | `mysql://user:password@localhost:3306/database`     | да         | Строка подключения Prisma к БД |
| `JWT_SECRET`                | `your_secret_key_min_32_chars`                      | да         | Секрет для подписи JWT (минимум 32 символа) |
| `JWT_EXPIRY`                | `24h`                                               | да         | Время жизни access‑токена |
| `RATE_LIMIT_ENABLED`        | `true`                                              | рек.       | Включить rate‑limiting |
| `RATE_LIMIT_MAX_ATTEMPTS`   | `5`                                                 | рек.       | Максимум попыток за окно времени |
| `RATE_LIMIT_WINDOW_MS`      | `60000`                                             | рек.       | Окно времени в миллисекундах |
| `RSA_PUBLIC_KEY_PATH`       | `./keys/public.key`                                 | рек.       | Путь к публичному RSA‑ключу |
| `RSA_PRIVATE_KEY_PATH`      | `./keys/private.key`                                | рек.       | Путь к приватному RSA‑ключу |
| `TEXTURE_PROVIDER`          | `mojang` / `ely.by` / `custom`                      | опц.       | Источник скинов/плащей |
| `TEXTURE_CACHE_ENABLED`     | `true`                                              | опц.       | Кеширование текстур |
| `COMPRESSION_ENABLED`       | `true`                                              | опц.       | Включить сжатие обновлений |
| `UPDATE_CHECK_INTERVAL`     | `300000`                                            | опц.       | Интервал проверки обновлений (мс) |
| `ECONOMY_LEADERBOARD_DEFAULT_LIMIT` | `5`                                         | опц.       | Лимит строк топа экономики по умолчанию |
| `ECONOMY_LEADERBOARD_MAX_LIMIT`     | `20`                                        | опц.       | Максимальный лимит строк топа экономики |
| `LOG_LEVEL`                 | `info` / `debug`                                    | опц.       | Минимальный уровень логирования |
| `UPDATES_DIR`               | `./updates`                                         | да         | Папка с клиентами/файлами обновлений |

> **Важно:** все эти переменные уже типизировано читаются в `packages/backend/src/config/index.ts`, используйте только этот модуль внутри кода.

### Frontend конфигурация

Все настройки фронтенда задаются через Vite‑переменные в файлах `.env` в `packages/frontend`:

- `.env.development` – для разработки (запуск `npm run dev:frontend`)  
- `.env.production` – для production‑сборки  
- `.env` – опционально, базовые значения по умолчанию

**Основные переменные:**

| Имя                    | Пример                        | Обязательна | Описание |
|------------------------|-------------------------------|------------|---------|
| `VITE_API_URL`         | `http://localhost:7240`       | да         | Базовый URL backend (без `/api`) |
| `VITE_WS_URL`          | `ws://localhost:7240`         | опц.       | Полный WebSocket URL; если не задан, вычисляется из `VITE_API_URL` |
| `VITE_DEV_SERVER_URL`  | `http://localhost:5173`       | опц.       | URL dev‑сервера Vite для Electron в dev‑режиме |

Frontend читает эти переменные исключительно через типизированные обёртки:

- `src/config/api.ts` — конфигурация HTTP/WS для React приложения (`API_CONFIG`)  
- `src/config/electron.ts` — конфигурация для Electron main‑процесса (`ELECTRON_CONFIG`)

### Конфигурация Electron

Настройки в `packages/frontend/package.json` в секции `build`:

- **appId** - уникальный идентификатор приложения
- **productName** - название продукта
- **icon** - путь к иконке
- **directories** - директории для сборки
- **win/linux/mac** - настройки для разных платформ

---

## 📖 Использование

### Управление пользователями

#### Через CLI (рекомендуется)

```bash
cd packages/backend
npm run cli

# Создать пользователя
user create username password --email user@example.com

# Создать администратора
user create admin admin123 --role ADMIN

# Список пользователей
user list

# Информация о пользователе
user info username

# Забанить пользователя
user ban username --reason "Violation of rules"

# Разбанить пользователя
user unban username

# Изменить роль
user role username ADMIN
```

#### Через API

```bash
# Регистрация
curl -X POST http://localhost:7240/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"user","password":"pass","email":"user@example.com"}'

# Вход
curl -X POST http://localhost:7240/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"user","password":"pass"}'
```

### Управление профилями серверов

#### Через CLI

```bash
# Создать профиль Vanilla
profile add "Vanilla Server" Vanilla 1.21.0

# Создать профиль Forge
profile add "HiTech" Forge 1.12.2 192.168.1.100 25565

# Создать профиль Fabric
profile add "HiPower" Fabric 1.20.1

# Список профилей
profile list

# Информация о профиле
profile info <profile-id>

# Включить/отключить профиль
profile enable <profile-id>
profile disable <profile-id>
```

#### Автоматическое скачивание клиента

```bash
# Скачать Vanilla клиент
client download "My Vanilla" Vanilla 1.21.0

# Скачать Forge клиент (автоматически установит Forge)
client download "Forge Server" Forge 1.12.2

# Скачать Fabric клиент (автоматически установит Fabric)
client download "Fabric Client" Fabric 1.20.1

# Скачать с настройками сервера
client download "My Server" Vanilla 1.20.4 192.168.1.100 25565
```

### Управление версиями клиента

```bash
# Синхронизировать файлы версии
version sync 1.12.2

# Проверить целостность файлов
version verify 1.12.2

# Статистика версии
version stats 1.12.2

# Включить/отключить версию
version enable 1.12.2
version disable 1.12.2
```

### Управление уведомлениями

```bash
# Отправить уведомление пользователю
notify send username SYSTEM_MESSAGE "Заголовок" "Текст сообщения"

# Отправить всем пользователям
notify send-all SYSTEM_MESSAGE "Техническое обслуживание" "Сервер будет недоступен с 00:00 до 02:00"

# Список уведомлений пользователя
notify list username --unread

# Очистить уведомления
notify clear username
```

### Просмотр статистики

```bash
# Статистика пользователей
stats users

# Статистика запусков за неделю
stats launches --days 7

# Статистика крашей
stats crashes --days 7

# Статистика сессий
stats sessions --days 30
```

### Просмотр базы данных

```bash
# Открыть Prisma Studio
cd packages/backend
npx prisma studio
# Откроется на http://localhost:5555
```

---

## 📡 API документация

### Авторизация

#### `POST /api/auth/register`
Регистрация нового пользователя.

**Тело запроса:**
```json
{
  "username": "string",
  "password": "string",
  "email": "string (опционально)"
}
```

**Ответ:**
```json
{
  "user": {
    "id": "uuid",
    "username": "string",
    "email": "string",
    "role": "USER"
  },
  "accessToken": "string",
  "refreshToken": "string"
}
```

#### `POST /api/auth/login`
Вход в систему.

**Тело запроса:**
```json
{
  "username": "string",
  "password": "string"
}
```

**Ответ:** Аналогичен `/api/auth/register`

#### `POST /api/auth/logout`
Выход из системы (требует авторизации).

#### `GET /api/auth/me`
Получить информацию о текущем пользователе (требует авторизации).

### Профили

#### `GET /api/profiles`
Получить список всех активных профилей.

**Ответ:**
```json
[
  {
    "id": "uuid",
    "title": "string",
    "version": "string",
    "description": "string",
    "tags": ["NEW", "TOP"],
    "serverAddress": "string",
    "serverPort": 25565,
    "enabled": true
  }
]
```

#### `GET /api/profiles/:id`
Получить детальную информацию о профиле.

### Серверы

#### `GET /api/servers/status`
Получить статус всех серверов.

**Ответ:**
```json
[
  {
    "address": "string",
    "port": 25565,
    "online": true,
    "players": {
      "online": 10,
      "max": 100
    },
    "version": "string",
    "ping": 50
  }
]
```

#### `GET /api/servers/:address/status`
Получить статус конкретного сервера.

#### `GET /api/servers/:address/statistics`
Получить статистику сервера за 24 часа.

**Ответ:**
```json
{
  "address": "string",
  "port": 25565,
  "statistics": [
    {
      "timestamp": "2024-01-01T00:00:00Z",
      "online": 10
    }
  ]
}
```

### Версии клиента

#### `GET /api/client-versions`
Получить список всех версий клиента.

#### `GET /api/client-versions/version/:version`
Получить информацию о версии по номеру.

#### `GET /api/client-versions/:id/files`
Получить список файлов версии.

#### `GET /api/client-versions/:id/files/:filePath`
Скачать файл версии.

### Обновления

#### `GET /api/updates/:profileId/check`
Проверить наличие обновлений для профиля.

#### `GET /api/updates/:profileId/files`
Получить список файлов для обновления.

### Уведомления

#### `GET /api/notifications`
Получить уведомления текущего пользователя (требует авторизации).

#### `POST /api/notifications/:id/read`
Пометить уведомление как прочитанное.

#### `DELETE /api/notifications/:id`
Удалить уведомление.

### Статистика

#### `GET /api/statistics/users`
Статистика пользователей.

#### `GET /api/statistics/launches?days=7`
Статистика запусков.

#### `GET /api/statistics/crashes?days=7`
Статистика крашей.

#### `GET /api/statistics/sessions?days=7`
Статистика сессий.

### WebSocket

Подключение: `ws://localhost:7240`

**События:**

- `UPDATE_PROGRESS` - прогресс загрузки файла
  ```json
  {
    "type": "UPDATE_PROGRESS",
    "profileId": "uuid",
    "filePath": "string",
    "progress": 50,
    "total": 100
  }
  ```

- `DOWNLOAD_CLIENT` - начало загрузки клиента
  ```json
  {
    "type": "DOWNLOAD_CLIENT",
    "profileId": "uuid",
    "status": "started"
  }
  ```

---

## 💻 CLI система управления

Лаунчер включает полнофункциональную CLI систему для управления всеми аспектами системы.

### Запуск CLI

```bash
cd packages/backend
npm run cli
```

### Основные команды

#### Системные команды

- `help [command]` - справка по командам
- `status` - статус системы
- `clear` / `cls` - очистить консоль
- `exit` / `quit` / `q` - выход

#### Управление пользователями

- `user list [--all]` - список пользователей
- `user create <username> <password> [--email <email>] [--role <USER|ADMIN>]` - создать пользователя
- `user delete <username>` - удалить пользователя
- `user ban <username> [--reason <reason>]` - забанить пользователя
- `user unban <username>` - разбанить пользователя
- `user role <username> <USER|ADMIN>` - изменить роль
- `user info <username>` - информация о пользователе

#### Управление профилями

- `profile list [--all]` - список профилей
- `profile add <title> <loader> <version> [serverAddress] [serverPort]` - создать профиль
- `profile info <id>` - информация о профиле
- `profile enable <id>` - включить профиль
- `profile disable <id>` - отключить профиль
- `profile delete <id>` - удалить профиль

#### Управление версиями клиента

- `version list [--all]` - список версий
- `version info <version>` - информация о версии
- `version sync <version>` - синхронизировать файлы
- `version verify <version>` - проверить целостность
- `version stats <version>` - статистика версии
- `version enable <version>` - включить версию
- `version disable <version>` - отключить версию

#### Автоматическое скачивание клиентов

- `client download <title> <loader> <version> [serverAddress] [serverPort]` - скачать клиент
- `client list` - список скачанных клиентов

#### Управление уведомлениями

- `notify send <username> <type> <title> <message>` - отправить уведомление
- `notify send-all <type> <title> <message>` - отправить всем
- `notify list <username> [--unread]` - список уведомлений
- `notify clear <username>` - очистить уведомления
- `notify types` - список типов уведомлений

#### Статистика

- `stats users` - статистика пользователей
- `stats launches [--days <n>]` - статистика запусков
- `stats crashes [--days <n>]` - статистика крашей
- `stats sessions [--days <n>]` - статистика сессий
- `stats errors [--days <n>]` - статистика ошибок

**Подробная документация:** См. [CLI_README.md](./build-scripts/CLI_README.md)

---

## 🔧 Разработка

### Структура разработки

1. **Shared пакет** - общие типы должны быть собраны первыми
2. **Backend** - серверная логика и API
3. **Frontend** - клиентское приложение

### Скрипты разработки

#### Корневой уровень

```bash
npm run dev              # Запуск backend + frontend
npm run dev:backend      # Только backend
npm run dev:frontend     # Только frontend
npm run build            # Сборка всех пакетов
npm run build:shared     # Сборка shared пакета
npm run lint             # Линтинг всех пакетов (если настроен в пакетах)
npm run test             # Запуск тестов во всех пакетах (если настроены)
npm run typecheck        # Проверка типов во всех пакетах (если настроено)
npm run format           # Форматирование кода во всех пакетах (если настроено)
```

> **Примечание:** устаревшие скрипты `dev:old`, `dev:backend:old`, `dev:frontend:old` были удалены.  
> Для разработки используйте актуальные команды `npm run dev`, `npm run dev:backend` и `npm run dev:frontend`.

#### Backend

```bash
cd packages/backend

npm run dev              # Запуск в режиме разработки (tsx watch)
npm run build            # Сборка TypeScript
npm run start            # Запуск production версии
npm run generate         # Генерация Prisma Client
npm run migrate          # Применение миграций
npm run studio           # Открыть Prisma Studio
npm run cli              # Запуск CLI
```

#### Frontend

```bash
cd packages/frontend

npm run dev              # Запуск Vite dev server
npm run build            # Сборка для production
npm run electron:dev     # Запуск Electron в dev режиме
npm run electron:build   # Сборка Electron приложения
npm run preview          # Предпросмотр production сборки
```

### Горячая перезагрузка

- **Backend**: Использует `tsx watch` для автоматической перезагрузки при изменении файлов
- **Frontend**: Использует Vite HMR для мгновенного обновления интерфейса

### Отладка

#### Backend

```bash
# Запуск с отладкой
node --inspect dist/index.js

# Или с tsx
tsx --inspect src/index.ts
```

#### Frontend (Electron)

В `electron/main.ts` можно использовать `console.log` для отладки main process.

Для renderer process используйте DevTools Electron.

### Тестирование

```bash
# Запуск тестов (если настроены)
npm test
```

---

## 📦 Сборка и развертывание

### Сборка для production

#### Backend

**Важно:** Перед сборкой backend убедитесь, что собран shared пакет:

```bash
# Из корня проекта
npm run build:shared
```

**Запуск в production:**

```bash
# Из корня проекта (рекомендуется)
npm run prod:backend

# Или из папки backend
cd packages/backend
npm run prod
```

Скрипт `prod:backend` автоматически:
1. Создаст/обновит `.env.prod` с production настройками (если не существует)
2. Скопирует `.env.prod` в `.env`
3. Соберёт TypeScript код (`npm run build`)
4. Запустит production сервер (`npm start`)

**Ручная сборка и запуск:**

```bash
cd packages/backend
npm run build
npm run start
```

#### Frontend

```bash
cd packages/frontend
npm run build
npm run electron:build
```

### Сборка Windows инсталлера из Linux

Для сборки Windows инсталлера из Linux системы:

#### Требования

- Wine (для NSIS установщика, опционально)
- NSIS (makensis, опционально)

#### Установка зависимостей

```bash
sudo apt-get update
sudo apt-get install -y wine nsis
```

#### Сборка

```bash
# Использование готового скрипта
./scripts/build-windows-installer.sh --auto-install

# Или вручную
cd packages/frontend
npm run build:portable    # Portable версия (рекомендуется)
npm run build:installer   # NSIS установщик
```

#### Результаты сборки

- **Portable версия** (рекомендуется): `packages/frontend/release/Modern Launcher-*-portable.exe`
  - Не требует установки
  - Работает стабильно на Linux
  - Не требует Wine/NSIS

- **NSIS установщик**: `packages/frontend/release/Modern Launcher-*-Setup.exe`
  - Полноценный установщик с деинсталлятором
  - Может иметь проблемы на Linux

### Развертывание на сервере

#### Backend

1. **Установите зависимости:**

```bash
# Из корня проекта
npm install

# Или только production зависимости
cd packages/backend
npm install --production
```

2. **Соберите shared пакет (обязательно):**

```bash
# Из корня проекта
npm run build:shared
```

3. **Настройте переменные окружения:**

```bash
cd packages/backend

# Скрипт prod:backend автоматически создаст .env.prod при первом запуске
# Или создайте вручную:
cp .env.example .env.prod
# Отредактируйте .env.prod с production настройками
```

**Основные переменные для production:**
- `NODE_ENV=production`
- `HOST` - IP адрес сервера (например, `5.188.119.206`)
- `PORT=7240`
- `CORS_ORIGIN` - разрешённые origin'ы (без `*` в production!)
- `DATABASE_URL` - строка подключения к MySQL
- `JWT_SECRET` - секретный ключ (минимум 32 символа)

4. **Примените миграции:**

```bash
cd packages/backend
npm run generate
npx prisma migrate deploy
```

5. **Запустите сервер:**

```bash
# Из корня проекта (рекомендуется - автоматически соберёт и запустит)
npm run prod:backend

# Или с PM2 для управления процессом (рекомендуется для production)
pm2 start dist/index.js --name launcher-backend --cwd packages/backend

# Или напрямую
cd packages/backend
npm run start
```

**Примечание:** Скрипт `prod:backend` автоматически собирает код перед запуском. Для PM2 сначала выполните сборку вручную.

#### Frontend

1. **Соберите приложение:**

```bash
npm run build
npm run electron:build
```

2. **Распространите файлы:**

- Windows: `release/Modern Launcher-*-portable.exe` или `*-Setup.exe`
- Linux: `release/Modern Launcher-*.AppImage` или `*.deb`
- macOS: `release/Modern Launcher-*.dmg`

### Обновление лаунчера

Система поддерживает автоматическое обновление лаунчера:

1. Создайте новую версию через CLI:
```bash
launcher create 1.0.225 --url http://example.com/launcher.exe --required
```

2. Лаунчер автоматически проверит обновления при запуске

---

## 🔒 Безопасность

### Реализованные меры безопасности

- ✅ **RSA шифрование** - защита критичных данных при передаче
- ✅ **JWT токены** - безопасная авторизация с истечением срока действия
- ✅ **bcrypt хеширование** - безопасное хранение паролей (10 rounds)
- ✅ **SHA-256 проверка** - валидация целостности файлов
- ✅ **Rate limiting** - защита от брутфорса и злоупотреблений
- ✅ **CORS настройки** - контроль доступа к API
- ✅ **Helmet.js** - защита HTTP заголовков
- ✅ **IP Whitelist/Blacklist** - управление доступом по IP
- ✅ **Система банов** - возможность банить пользователей
- ✅ **Роли пользователей** - разделение прав доступа

### Рекомендации по безопасности

1. **Используйте сильные секретные ключи:**
   - `JWT_SECRET` должен быть минимум 32 символа
   - Используйте случайные строки

2. **Настройте CORS правильно:**
   - В production укажите конкретные домены
   - Не используйте `*` в production

3. **Защитите базу данных:**
   - Используйте отдельного пользователя БД с ограниченными правами
   - Регулярно делайте резервные копии

4. **Настройте rate limiting:**
   - Адаптируйте под вашу нагрузку
   - Мониторьте попытки брутфорса

5. **Используйте HTTPS:**
   - В production обязательно используйте HTTPS
   - Настройте SSL сертификаты

6. **Регулярно обновляйте зависимости:**
   ```bash
   npm audit
   npm audit fix
   ```

---

## 🐛 Устранение неполадок

### Сервер не запускается

**Проблема:** Backend не запускается

**Решения:**
1. Проверьте логи: `pm2 logs launcher-backend` или консоль
2. Убедитесь, что порт 7240 свободен: `lsof -i :7240`
3. Проверьте подключение к базе данных
4. Проверьте переменные окружения в `.env`
5. Убедитесь, что миграции применены: `npx prisma migrate deploy`

### Ошибки при скачивании файлов

**Проблема:** Файлы не скачиваются или ошибки при загрузке

**Решения:**
1. Проверьте права доступа к директории `updates/`
2. Убедитесь, что файлы существуют на сервере
3. Проверьте хеши в базе данных
4. Проверьте логи WebSocket соединения
5. Убедитесь, что путь к файлам правильный

### Проблемы с запуском игры

**Проблема:** Игра не запускается или крашится

**Решения:**
1. Убедитесь, что Java установлена и доступна в PATH
2. Проверьте версию Java (должна соответствовать версии Minecraft)
3. Проверьте пути к нативным библиотекам
4. Проверьте логи игры в модальном окне
5. Убедитесь, что все файлы клиента загружены
6. Проверьте настройки профиля (mainClass, classPath, jvmArgs)

### Проблемы с авторизацией

**Проблема:** Не могу войти в систему

**Решения:**
1. Проверьте правильность username/password
2. Проверьте, не забанен ли пользователь
3. Проверьте JWT_SECRET в `.env`
4. Проверьте логи backend на наличие ошибок
5. Очистите localStorage в браузере/Electron

### Проблемы с WebSocket

**Проблема:** WebSocket не подключается

**Решения:**
1. Проверьте, что WebSocket сервер запущен
2. Проверьте URL WebSocket в конфигурации frontend
3. Проверьте CORS настройки
4. Проверьте файрвол и прокси-серверы
5. Проверьте логи WebSocket на backend

### Проблемы с базой данных

**Проблема:** Ошибки при работе с БД

**Решения:**
1. Проверьте подключение к MySQL
2. Убедитесь, что все миграции применены
3. Проверьте права доступа пользователя БД
4. Проверьте логи Prisma
5. Используйте Prisma Studio для диагностики: `npx prisma studio`

### Проблемы со сборкой

**Проблема:** Ошибки при сборке приложения

**Решения:**
1. Убедитесь, что все зависимости установлены: `npm install`
2. Соберите shared пакет: `npm run build:shared`
3. Очистите кеш: `rm -rf node_modules package-lock.json && npm install`
4. Проверьте версию Node.js: `node --version` (должна быть >= 20.0.0)
5. Проверьте логи сборки на наличие ошибок
6. Для TailwindCSS 4.x убедитесь, что установлен `@tailwindcss/postcss`

### Проблемы совместимости после обновления

**Проблема:** Ошибки после обновления зависимостей

**Решения:**
1. **Express 5.x**: Wildcard routes (`/*`) теперь используют query параметры (`?path=...`)
2. **Prisma 6.x**: Убедитесь, что `DATABASE_URL` правильно настроен в `.env`
3. **TailwindCSS 4.x**: Используйте `@tailwindcss/postcss` в `postcss.config.js`
4. **React 19**: Проверьте совместимость кастомных хуков и компонентов
5. **React Router 7.x**: Обновите навигацию, если используете устаревшие API

---

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста, следуйте этим рекомендациям:

### Процесс вклада

1. **Fork репозитория**
2. **Создайте ветку** для вашей функции: `git checkout -b feature/amazing-feature`
3. **Сделайте коммит** изменений: `git commit -m 'Add amazing feature'`
4. **Push в ветку**: `git push origin feature/amazing-feature`
5. **Создайте Pull Request**

### Стандарты кода

- Используйте TypeScript для всех новых файлов
- Следуйте существующему стилю кода
- Добавляйте комментарии для сложной логики
- Пишите понятные имена переменных и функций
- Тестируйте изменения перед коммитом

### Сообщения о багах

При создании issue о баге, пожалуйста, укажите:

- Версию Node.js
- Версию лаунчера
- Описание проблемы
- Шаги для воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Скриншоты (если применимо)
- Логи (если есть)

---

## 📄 Лицензия

Этот проект лицензирован под **GNU GPL v3.0**. См. файл [LICENSE](./LICENSE) для подробностей.

---

## 👨‍💻 Авторы

- **Основано на**: LauncherSchool (sashok724 v3)
- **Современная реализация**: 2024
- **Репозиторий**: [GitHub](https://github.com/xuviga/LauncherSchool-sashok724-v3-Fork)

---

## 🌟 Особенности реализации

- ✅ **Модульная архитектура** - легко расширяемая кодовая база
- ✅ **TypeScript** - полная типизация для надежности
- ✅ **Monorepo** - единый репозиторий для всех пакетов
- ✅ **Real-time обновления** - WebSocket для мгновенных уведомлений
- ✅ **Статистика серверов** - хранение и визуализация данных за 24 часа
- ✅ **Автоматическая обработка** - нативные библиотеки, проверка целостности
- ✅ **Современный UI** - красивый и интуитивный интерфейс
- ✅ **CLI управление** - полнофункциональная командная строка
- ✅ **Автоматическое скачивание** - автоматическая установка Forge/Fabric
- ✅ **Поддержка всех версий** - от Minecraft 1.2.5 до последней версии
- ✅ **Актуальные технологии** - React 19, Express 5, Prisma 6, TailwindCSS 4
- ✅ **Оптимизированная производительность** - последние версии всех зависимостей

---

## 📚 Дополнительная документация

### Техническая документация

- [CLI README](./build-scripts/CLI_README.md) - Подробная документация по CLI системе
- [CLIENT BUILD GUIDE](./build-scripts/CLIENT_BUILD_GUIDE.md) - Руководство по сборке клиентов
- [INSTALLER NOTE](./packages/frontend/INSTALLER_NOTE.md) - Заметки по установщику
- [BUILD INSTALLER](./packages/frontend/BUILD_INSTALLER.md) - Инструкции по сборке установщика
- [BUILD.md](./BUILD.md) - Руководство по сборке и развертыванию
- [SETUP_QUALITY.md](./SETUP_QUALITY.md) - Настройка инструментов качества кода

### Архитектурные решения (ADR)

- [ADR-001: Electron + React + Vite](./docs/adr/001-electron-react-vite.md) - Обоснование выбора технологического стека
- [ADR-002: Prisma ORM](./docs/adr/002-prisma-orm.md) - Выбор подхода к работе с БД
- [ADR-003: Стратегия обновлений лаунчера](./docs/adr/003-launcher-update-strategy.md) - Механизм обновлений

### Для разработчиков

- [CONTRIBUTING.md](./CONTRIBUTING.md) - Руководство по внесению вклада в проект
- [Frontend Architecture](./packages/frontend/FRONTEND_ARCHITECTURE.md) - Архитектура frontend
- [Backend Architecture](./packages/backend/BACKEND_ARCHITECTURE.md) - Архитектура backend

---

## 🆘 Поддержка

При возникновении проблем или вопросов:

1. Проверьте [Устранение неполадок](#-устранение-неполадок)
2. Проверьте логи сервера
3. Используйте команду `status` в CLI для диагностики
4. Обратитесь к документации API
5. Создайте issue на GitHub

---

**Сделано с ❤️ для сообщества Minecraft**

---

*Версия документации: 1.0.0*  
*Последнее обновление: 2025-01-30*

---

## 🔄 История обновлений

### Версия 1.0.0 (2025-01-30)

#### Обновления зависимостей

**Frontend:**
- ⬆️ React: 18.2.0 → 19.2.0
- ⬆️ Electron: 28.1.3 → 39.2.4
- ⬆️ Vite: 5.0.11 → 7.2.4
- ⬆️ TailwindCSS: 3.4.1 → 4.1.17
- ⬆️ Zustand: 4.4.7 → 5.0.8
- ⬆️ React Router: 6.21.1 → 7.9.6
- ⬆️ Framer Motion: 10.18.0 → 12.23.24
- ⬆️ React Query: 5.17.9 → 5.90.11

**Backend:**
- ⬆️ Express: 4.18.2 → 5.1.0
- ⬆️ Prisma: 5.8.1 → 6.0.0
- ⬆️ bcrypt: 5.1.1 → 6.0.0
- ⬆️ Helmet: 7.1.0 → 8.1.0
- ⬆️ express-rate-limit: 7.1.5 → 8.2.1
- ⬆️ Zod: 3.22.4 → 4.1.13
- ⬆️ Chokidar: 4.0.3 → 5.0.0

**Общие:**
- ⬆️ TypeScript: 5.3.3 → 5.9.3
- ⬆️ Node.js: >=18.0.0 → >=20.0.0
- ⬆️ npm: >=9.0.0 → >=10.0.0

#### Исправления совместимости

- ✅ Исправлена обработка wildcard routes в Express 5.x (переход на query параметры)
- ✅ Обновлена конфигурация Prisma 6.x
- ✅ Обновлен PostCSS плагин для TailwindCSS 4.x (`@tailwindcss/postcss`)
- ✅ Адаптированы API вызовы для совместимости с Express 5.x
- ✅ Обновлены типы React 19

#### Улучшения

- 🚀 Улучшена производительность благодаря обновленным зависимостям
- 🔒 Повышена безопасность с новыми версиями Helmet и express-rate-limit
- 📦 Оптимизированы размеры бандлов
- 🎨 Улучшена поддержка современных возможностей React 19
