# CLI Command System - Документация

> 📖 **Документация по сборке клиентов:** См. [CLIENT_BUILD_GUIDE.md](./CLIENT_BUILD_GUIDE.md) для подробных инструкций по подготовке и сборке клиентов различных версий (Vanilla, Forge, Fabric).

## Введение

Профессиональная система командной строки (CLI) для управления Modern Minecraft Launcher. CLI предоставляет администраторам удобный интерфейс для управления пользователями, профилями, версиями клиентов, файлами, уведомлениями и статистикой.

## Запуск CLI

### Способ 1: Через npm скрипт (рекомендуется)
```bash
cd packages/backend
npm run cli
```

### Способ 2: Напрямую через tsx
```bash
cd packages/backend
tsx src/cli/index.ts
```

### Способ 3: Через скомпилированный код
```bash
cd packages/backend
npm run build
node dist/cli/index.js
```

**Важно:** CLI запускается в отдельном процессе и не мешает работе основного сервера. Вы можете запускать CLI параллельно с работающим сервером.

## Основные команды

### Системные команды

#### `help [command]`
Показать справку по всем командам или конкретной команде.

**Примеры:**
```bash
help              # Показать все доступные команды
help user         # Показать справку по командам пользователей
help version      # Показать справку по командам версий
```

#### `status`
Показать статус системы: подключение к БД, статистику, использование памяти, время работы.

**Пример:**
```bash
status
```

#### `clear` / `cls`
Очистить консоль.

**Пример:**
```bash
clear
```

#### `exit` / `quit` / `q`
Выход из CLI.

---

## Команды управления пользователями

### `user list [--all]`
Список всех пользователей. Флаг `--all` включает забаненных пользователей.

**Примеры:**
```bash
user list
user list --all
```

### `user create <username> <password> [--email <email>] [--role <USER|ADMIN>]`
Создать нового пользователя.

**Параметры:**
- `<username>` - имя пользователя (обязательно)
- `<password>` - пароль (обязательно)
- `--email <email>` - email адрес (опционально)
- `--role <USER|ADMIN>` - роль пользователя (по умолчанию: USER)

**Примеры:**
```bash
user create testuser password123
user create admin admin123 --role ADMIN
user create user1 pass123 --email user1@example.com
```

### `user delete <username>`
Удалить пользователя. Требует подтверждения.

**Пример:**
```bash
user delete testuser
```

### `user ban <username> [--reason <reason>]`
Забанить пользователя.

**Параметры:**
- `<username>` - имя пользователя
- `--reason <reason>` - причина бана (опционально)

**Примеры:**
```bash
user ban testuser
user ban testuser --reason "Violation of rules"
```

### `user unban <username>`
Разбанить пользователя.

**Пример:**
```bash
user unban testuser
```

### `user role <username> <USER|ADMIN>`
Изменить роль пользователя.

**Примеры:**
```bash
user role testuser ADMIN
user role admin USER
```

### `user info <username>`
Показать подробную информацию о пользователе.

**Пример:**
```bash
user info testuser
```

---

## Команды управления профилями

### `profile list [--all]`
Список всех профилей клиентов. Флаг `--all` включает отключенные профили.

**Примеры:**
```bash
profile list
profile list --all
```

### `profile add <title> <loader> <version> [serverAddress] [serverPort]`
Создать новый профиль клиента с поддержкой Forge, Fabric или Vanilla.

**Параметры:**
- `<title>` - Название профиля (например, "HiTech", "My Server")
- `<loader>` - Модлоадер: `Forge`, `Fabric` или `Vanilla`
- `<version>` - Версия Minecraft (например, "1.12.2", "1.20.1", "1.21.0")
- `[serverAddress]` - IP адрес сервера (опционально, по умолчанию: "localhost")
- `[serverPort]` - Порт сервера (опционально, по умолчанию: 25565)

**Примеры:**
```bash
profile add "HiTech" Forge 1.12.2
profile add "My Server" Fabric 1.20.1 192.168.1.100 25565
profile add "Vanilla" Vanilla 1.21.0
profile add "Forge Server" Forge 1.16.5 5.188.119.206 25565
```

**Примечания:**
- Для Forge 1.12.2: Использует LaunchWrapper с FMLTweaker
- Для Forge 1.16+: Использует BootstrapLauncher
- Для Fabric: Использует Fabric Loader (рекомендуется для версий 1.16+)
- Версия JVM автоматически выбирается на основе версии Minecraft:
  - 1.21+: Java 21
  - 1.17-1.20: Java 17
  - 1.12-1.16: Java 8

### `profile info <id>`
Показать подробную информацию о профиле.

**Пример:**
```bash
profile info <profile-id>
```

### `profile enable <id>`
Включить профиль.

**Пример:**
```bash
profile enable <profile-id>
```

### `profile disable <id>`
Отключить профиль.

**Пример:**
```bash
profile disable <profile-id>
```

### `profile delete <id>`
Удалить профиль. Требует подтверждения.

**Пример:**
```bash
profile delete <profile-id>
```

---

## Команды управления версиями клиентов

### `version list [--all]`
Список всех версий клиентов. Флаг `--all` включает отключенные версии.

**Примеры:**
```bash
version list
version list --all
```

### `version info <version>`
Показать подробную информацию о версии клиента.

**Пример:**
```bash
version info 1.12.2
```

### `version sync <version>`
Синхронизировать файлы версии из директории `updates` с базой данных.

**Пример:**
```bash
version sync 1.12.2
```

### `version verify <version>`
Проверить целостность всех файлов версии.

**Пример:**
```bash
version verify 1.12.2
```

### `version stats <version>`
Показать статистику синхронизации для версии.

**Пример:**
```bash
version stats 1.12.2
```

### `version enable <version>`
Включить версию клиента.

**Пример:**
```bash
version enable 1.12.2
```

### `version disable <version>`
Отключить версию клиента.

**Пример:**
```bash
version disable 1.12.2
```

---

## Команды управления файлами

### `file sync <version>`
Синхронизировать файлы версии из директории `updates` с базой данных.

**Пример:**
```bash
file sync 1.12.2
```

### `file verify <version>`
Проверить целостность всех файлов версии.

**Пример:**
```bash
file verify 1.12.2
```

### `file stats <version>`
Показать статистику файлов для версии.

**Пример:**
```bash
file stats 1.12.2
```

### `file list <version>`
Показать список всех файлов версии.

**Пример:**
```bash
file list 1.12.2
```

---

## Команды управления версиями лаунчера

### `launcher list [--all]`
Список всех версий лаунчера. Флаг `--all` включает отключенные версии.

**Примеры:**
```bash
launcher list
launcher list --all
```

### `launcher info <version>`
Показать подробную информацию о версии лаунчера.

**Пример:**
```bash
launcher info 1.0.224
```

### `launcher create <version> [--url <url>] [--required]`
Создать новую версию лаунчера.

**Параметры:**
- `<version>` - версия лаунчера (обязательно)
- `--url <url>` - URL для загрузки (опционально)
- `--required` - пометить как обязательное обновление

**Примеры:**
```bash
launcher create 1.0.225
launcher create 1.0.225 --url http://example.com/launcher.exe
launcher create 1.0.225 --url http://example.com/launcher.exe --required
```

### `launcher enable <version>`
Включить версию лаунчера.

**Пример:**
```bash
launcher enable 1.0.225
```

### `launcher disable <version>`
Отключить версию лаунчера.

**Пример:**
```bash
launcher disable 1.0.224
```

### `launcher delete <version>`
Удалить версию лаунчера. Требует подтверждения.

**Пример:**
```bash
launcher delete 1.0.224
```

---

## Команды управления уведомлениями

### `notify send <username> <type> <title> <message>`
Отправить уведомление конкретному пользователю.

**Параметры:**
- `<username>` - имя пользователя
- `<type>` - тип уведомления (см. `notify types`)
- `<title>` - заголовок уведомления
- `<message>` - текст уведомления

**Доступные типы:**
- `CLIENT_UPDATE_AVAILABLE`
- `SERVER_STATUS_CHANGE`
- `LAUNCHER_UPDATE_AVAILABLE`
- `GAME_CRASH`
- `CONNECTION_ISSUE`
- `LAUNCHER_ERROR`
- `SYSTEM_MESSAGE`
- `ADMIN_ALERT`

**Пример:**
```bash
notify send testuser SYSTEM_MESSAGE "Maintenance" "Server maintenance in 1 hour"
```

### `notify send-all <type> <title> <message>`
Отправить уведомление всем пользователям.

**Пример:**
```bash
notify send-all SYSTEM_MESSAGE "Maintenance" "Server maintenance in 1 hour"
```

### `notify list <username> [--unread]`
Показать список уведомлений пользователя. Флаг `--unread` показывает только непрочитанные.

**Примеры:**
```bash
notify list testuser
notify list testuser --unread
```

### `notify clear <username>`
Очистить все уведомления пользователя.

**Пример:**
```bash
notify clear testuser
```

### `notify types`
Показать список доступных типов уведомлений.

**Пример:**
```bash
notify types
```

---

## Команды статистики

### `stats users`
Показать статистику пользователей: общее количество, активные, забаненные, администраторы.

**Пример:**
```bash
stats users
```

### `stats launches [--days <n>]`
Показать статистику запусков игры. Флаг `--days` ограничивает период.

**Примеры:**
```bash
stats launches
stats launches --days 7
stats launches --days 30
```

### `stats crashes [--days <n>]`
Показать статистику крашей игры.

**Примеры:**
```bash
stats crashes
stats crashes --days 7
```

### `stats sessions [--days <n>]`
Показать статистику игровых сессий.

**Примеры:**
```bash
stats sessions
stats sessions --days 7
```

### `stats errors [--days <n>]`
Показать статистику ошибок лаунчера.

**Примеры:**
```bash
stats errors
stats errors --days 7
```

---

## Команды скачивания клиентов

### `client download <title> <loader> <version> [serverAddress] [serverPort]`

Автоматически скачивает клиент Minecraft с официальных источников и создает профиль.

**Параметры:**
- `title` - Название профиля (будет использовано для создания папки)
- `loader` - Тип загрузчика: `Vanilla`, `Forge`, или `Fabric`
- `version` - Версия Minecraft (например, `1.12.2`, `1.20.1`, `1.21.0`)
- `serverAddress` - (опционально) Адрес сервера по умолчанию
- `serverPort` - (опционально) Порт сервера по умолчанию (1-65535)

**Примеры:**
```bash
# Скачать Vanilla клиент
client download "My Vanilla" Vanilla 1.21.0

# Скачать Forge клиент
client download "Forge Server" Forge 1.12.2

# Скачать Fabric клиент
client download "Fabric Client" Fabric 1.20.1

# Скачать с настройками сервера
client download "My Server" Vanilla 1.20.4 192.168.1.100 25565
```

**Что делает команда:**
1. ✅ Скачивает `client.jar` с официального сайта Mojang
2. ✅ Скачивает все необходимые библиотеки (libraries)
3. ✅ **Автоматически устанавливает Forge/Fabric loader** (для Forge и Fabric клиентов)
4. ✅ Скачивает assets (ресурсы) для указанной версии
5. ✅ Создает профиль с правильными настройками для выбранного загрузчика
6. ✅ Автоматически настраивает `mainClass`, `classPath`, `jvmArgs`, `clientArgs`
7. ✅ Синхронизирует файлы с базой данных

**Примечания:**
- Для **Forge** и **Fabric** автоматически скачивается и устанавливается соответствующий loader - **ручная установка не требуется!**
- Все файлы сохраняются в `updates/<client-directory>/`
- Assets сохраняются в общей папке `updates/assets/<assetIndex>/` (один раз для всех клиентов)
- После скачивания можно добавить моды в папку `updates/<client-directory>/mods/`
- Клиент готов к использованию сразу после скачивания

### `client list`

Показывает список всех автоматически скачанных клиентов.

**Пример:**
```bash
client list
```

**Вывод:**
- ID профиля
- Название
- Версия Minecraft
- Имя директории
- Статус (включен/выключен)
- Дата создания

---

## Особенности

### Автодополнение
CLI поддерживает автодополнение команд. Нажмите `Tab` для автодополнения.

### Цветной вывод
CLI использует цветной вывод для лучшей читаемости:
- 🟢 Зеленый - успешные операции
- 🔴 Красный - ошибки
- 🟡 Желтый - предупреждения
- 🔵 Синий - информационные сообщения

### Таблицы
Многие команды выводят данные в виде таблиц для удобного просмотра.

### Подтверждение опасных операций
Команды, которые удаляют данные (например, `user delete`, `profile delete`), требуют подтверждения.

---

## Примеры использования

### Создание администратора
```bash
user create admin admin123 --role ADMIN --email admin@example.com
```

### Синхронизация новой версии клиента
```bash
version sync 1.20.1
version verify 1.20.1
version enable 1.20.1
```

### Отправка уведомления о техническом обслуживании
```bash
notify send-all SYSTEM_MESSAGE "Техническое обслуживание" "Сервер будет недоступен с 00:00 до 02:00"
```

### Проверка статистики за неделю
```bash
stats launches --days 7
stats crashes --days 7
stats sessions --days 7
```

---

## Устранение неполадок

### CLI не запускается
1. Убедитесь, что база данных доступна
2. Проверьте переменные окружения в `.env`
3. Проверьте логи сервера

### Команда не найдена
- Используйте `help` для просмотра всех доступных команд
- Проверьте правильность написания команды
- Убедитесь, что используете правильные подкоманды

### Ошибки базы данных
- Проверьте подключение к базе данных
- Убедитесь, что все миграции применены
- Проверьте права доступа к базе данных

---

## Дополнительная информация

CLI интегрирован с основными сервисами лаунчера:
- **File Sync Service** - автоматическая синхронизация файлов
- **Notification Service** - управление уведомлениями
- **Statistics Service** - сбор и анализ статистики
- **Auth Service** - управление пользователями и аутентификацией

Все изменения, сделанные через CLI, немедленно отражаются в системе и доступны через API и веб-интерфейс.

---

## Поддержка

При возникновении проблем или вопросов:
1. Проверьте логи сервера
2. Используйте команду `status` для диагностики
3. Обратитесь к документации API

---

**Версия:** 1.0.0  
**Дата обновления:** 2025-01-29

