# Root .env example (optional)
# Copy this to .env if you want to set global environment variables

NODE_ENV=development

# Backend will use packages/backend/.env
# Frontend will use packages/frontend/.env (if needed)
